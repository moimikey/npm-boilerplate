//import { typeov } from 'typeov'
import tap from 'tap'

const describe = tap.test

describe('PACKAGE_NAME', i => {
  const it = i.test

  it('Logic', async t => {
    // t.plan(2)
    t.equal(true, true)
    t.end()
  })

  i.end()

})
